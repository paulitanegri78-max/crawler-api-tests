{
  "name": "projetoaxur",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --file tests/setup.js tests/**/*.spec.js",
    "test:report": "mocha --file tests/setup.js tests/**/*.spec.js --reporter mochawesome --reporter-options reportDir=reports,reportFilename=mochawesome-report,quiet=true,overwrite=true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "directories": {
    "test": "tests"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "mocha": "^11.7.6",
    "mochawesome": "^7.1.4",
    "supertest": "^7.2.2"
  }
}
